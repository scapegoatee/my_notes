git init   # creates a .git folder in current directory. First time setup.
git remote add origin https://github.com/scapegoatee/my_notes.git

	
git add .
git status
git commit -m "message"
git log
git push origin master

cat .git/config
